클래스 -> 자바 프로그램을 이루는 최소단위
ㄴ 필드(상태), 메서드(기능), 생성자(객체를 메모리에 로딩해준다.)

객체 -> 클래스를 통해 만들어진 것
ㄴ 클래스를통해 객체를 여러개 만드는것이 가능
여러개의 객체가 각강 필드와 메서드를 가지게 된다.

오버로딩 -> 상속이랑 상관없다
메서드의 다중 로딩
외부에서 받을 수 있는 매개변수의 개수만큼 정의
1.매개변수의 개수가 다를때
2.매개변수의 개수가 같을 때 자료형이 다르다
3.2개의 매개변수가 있을 때 순서가 다르다
4.메서드,생성자 이름을 그대로 가져간다.

오버라이딩 -> 상속이랑 상관 있음

상속
부모클래스에 정의된 필드, 메서드를 자식클래스에서 이어받아서 쓸 수 있는것.

자식클래스의 객체가 만들어졌을 때 부모클래스의 필드와 메서드를 사용할 수 있다는 것.

부모클래스 : 공통의 개념이 들어간다, 일반적인 개념

자식클래스 : 구체적인 개념, 자식클래스만이 가지고 있는 개념

상속
오버라이딩
super
2차상속
final

다형성

클래스에서의 타입변환
1. 기본 자료형의 변환








































